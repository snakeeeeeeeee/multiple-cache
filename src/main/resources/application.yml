spring:
  redis:
    port: 6384 # redis server port
    host: 10.25.246.26 # redis server host
    lettuce:
      pool:
        max-active: 50
        max-wait: 2000
        max-idle: 20
        min-idle: 5
  application:
    name: aaaaaaaaaa

multiple-cache:
#  redis:
#    - name: testName2
#      expire: 10000
#      strategy: defaultRedisCacheStrategy
#      decorators: demoCachedValueRebuilder
#    - name: testName3
#      expire: 10000
#      strategy: defaultRedisCacheStrategy
#      decorators: demoCachedValueRebuilder
#  caffeine:
#    - name: testName2
#      expireAfterAccess: 30
#      initialCapacity: 100
#      maximumSize: 1000
  multiple:
    - name: testName2
      caffeine:
        expireAfterAccess: 30
        initialCapacity: 100
        maximumSize: 1000
      redis:
        expire: 10000

logging:
  level:
    com:
      zy:
        github:
          multiple:
            cache:
              sync: debug


