spring:
  redis:
    port: 6384 # redis server port
    host: 10.25.246.26 # redis server host
    lettuce:
      pool:
        max-active: 50
        max-wait: 2000
        max-idle: 20
        min-idle: 5
  application:
    name: aaaaaaaaaa
multiple-cache:
#  redis:
#    - name: testCache
#      expire: 100
  caffeine:
    - name: testCache
      expireAfterAccess: 30
      initialCapacity: 100
      maximumSize: 1000
#  multiple:
#    - name: testCache
#      caffeine:
#        expireAfterAccess: 30
#        initialCapacity: 100
#        maximumSize: 1000
#      redis:
#        expire: 100

logging:
  level:
    com:
      zy:
        github:
          multiple:
            cache:
              sync: debug


